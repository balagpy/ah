<p class="lead">Welcome <%= @resource.fullname %></p>
<p>You have successfully registered the account of <%= link_to Setting.app_name, root_url %>, 
  Next you need to click on the link below to <span class="text-danger">activate</span> your account:</p>
<p><%= link_to'Activate account', confirmation_url(@resource, confirmation_token: @token), class:'btn' %></p>
<p>If you don’t see the link, please copy the link below and open it in your browser:</p>
<p><%= confirmation_url(@resource, confirmation_token: @token) %></p>
